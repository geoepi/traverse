#' Spatial passage across gridded landscapes
#'
#' `traverse` estimates passage between source and destination sets while
#' separating the scientific target domain from a larger computational domain.
#' External terminal zones reduce the risk that high values at path origins and
#' destinations obscure the area of interpretation. The computational result is
#' retained for diagnostics, while the default interpreted result is masked back
#' to the exact target template.
#'
#' @keywords internal
"_PACKAGE"
