% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/passage.R
\name{traverse_passage}
\alias{traverse_passage}
\title{Calculate randomized-shortest-path passage}
\usage{
traverse_passage(
  surface,
  sources,
  targets,
  pairs = NULL,
  directions = 16,
  theta = 1,
  correction = "c",
  scale_correction = TRUE,
  aggregate = TRUE,
  aggregation = c("mean", "sum"),
  workers = 1
)
}
\arguments{
\item{surface}{A \code{traverse_surface} or single-layer conductance
\link[terra:SpatRaster-class]{terra::SpatRaster}.}

\item{sources}{Source points as a \link[terra:SpatVector-class]{terra::SpatVector} or \code{sf} object.}

\item{targets}{Target points as a \link[terra:SpatVector-class]{terra::SpatVector} or \code{sf} object.}

\item{pairs}{Optional output of \code{\link[=traverse_pairs]{traverse_pairs()}}. Defaults to all pairs.}

\item{directions}{Neighborhood size passed to \code{gdistance} (4, 8, or 16).}

\item{theta}{Positive randomized-shortest-path parameter.}

\item{correction}{Geographic correction type passed to \code{gdistance}, or
\code{NULL} to omit correction.}

\item{scale_correction}{Logical; whether to apply \code{gdistance} geographic
correction scaling. \code{TRUE} reproduces the historical prototype behavior
and interacts with the interpretation of \code{theta}. Scaling changes the
numerical scale of corrected transition values and can be disabled with
\code{FALSE}.}

\item{aggregate}{Logical; if \code{TRUE}, return an aggregated raster. If
\code{FALSE}, return pairwise rasters and metadata.}

\item{aggregation}{Either \code{"mean"} or \code{"sum"} when aggregating.}

\item{workers}{Explicit worker count reserved for future parallel execution.
The safe default is one worker.}
}
\value{
A \link[terra:SpatRaster-class]{terra::SpatRaster} when \code{aggregate = TRUE}; otherwise a list with
pairwise rasters and metadata.
}
\description{
Passage is reported as passage intensity or relative passage, not as a
probability. \code{theta} controls the randomized-shortest-path behavior and is
a scientific model parameter. It is intentionally configurable; no value is
claimed to be universally optimal. Resistance is converted internally to
conductance as \code{1 / resistance} for the \code{gdistance} backend.
}
