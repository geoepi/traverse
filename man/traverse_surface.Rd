% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/surface.R
\name{traverse_surface}
\alias{traverse_surface}
\title{Prepare a conductance or resistance surface}
\usage{
traverse_surface(
  x,
  domain = NULL,
  type = c("conductance", "resistance"),
  transform = NULL,
  minimum = NULL,
  outside_value = NULL
)
}
\arguments{
\item{x}{A single-layer \link[terra:SpatRaster-class]{terra::SpatRaster}.}

\item{domain}{Optional \code{traverse_domain} whose computational geometry should
be returned.}

\item{type}{Either \code{"conductance"} or \code{"resistance"}.}

\item{transform}{Optional function applied to \code{x} before alignment.}

\item{minimum}{Optional lower bound applied to finite values.}

\item{outside_value}{Fill value used when extending a surface to the
computational domain. It is required when extension is needed.}
}
\value{
An object of class \code{traverse_surface} containing the aligned raster,
its semantic type, and provenance metadata.
}
\description{
Conductance expresses ease of movement (higher values are easier); resistance
expresses movement cost (higher values are harder). No normalization or
project-specific multiplication is performed automatically.
Finite zero values are retained as raw/prepared surface data. Before passage,
finite conductance values must be made strictly positive with an explicit
\code{minimum}, \code{transform}, or \code{NA} barrier decision; the backend never assigns a
conductance floor silently.
}
