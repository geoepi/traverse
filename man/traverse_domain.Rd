% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/domain.R
\name{traverse_domain}
\alias{traverse_domain}
\title{Define target and computational domains}
\usage{
traverse_domain(template, buffer, buffer_units = c("map", "cells"))
}
\arguments{
\item{template}{A single-layer \link[terra:SpatRaster-class]{terra::SpatRaster}. Finite cells define the
target domain; \code{NA} and non-finite cells are excluded.}

\item{buffer}{Non-negative buffer distance. A positive value is required for
a computational domain larger than the target.}

\item{buffer_units}{Either \code{"map"} for map units or \code{"cells"} for cell
counts. Map-unit distances require a projected CRS.}
}
\value{
An object of class \code{traverse_domain}.
}
\description{
A target domain is the area of scientific interpretation. The computational
domain expands that geometry by a buffer so terminals can be placed outside
the target and terminal artifacts can be masked from final outputs.
}
\examples{
template <- terra::rast(nrows = 5, ncols = 6, xmin = 0, xmax = 6,
  ymin = 0, ymax = 5, crs = "EPSG:3857")
terra::values(template) <- 1
domain <- traverse_domain(template, buffer = 2, buffer_units = "cells")
domain
}
