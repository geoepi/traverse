% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nodes.R
\name{traverse_nodes}
\alias{traverse_nodes}
\title{Generate external terminal nodes}
\usage{
traverse_nodes(
  domain,
  side = c("south", "north"),
  distance,
  n,
  placement = c("even", "random"),
  allocation = c("equal", "boundary_length"),
  seed = NULL
)
}
\arguments{
\item{domain}{A \code{traverse_domain} object.}

\item{side}{One or more of \code{"south"}, \code{"north"}, \code{"east"}, and \code{"west"}.}

\item{distance}{Positive distance in map units outside the target edge.}

\item{n}{A positive scalar total or a named count for each side.}

\item{placement}{Either \code{"even"} or \code{"random"}.}

\item{allocation}{For scalar \code{n} across multiple sides, either \code{"equal"}
or \code{"boundary_length"}.}

\item{seed}{Optional random seed used only for random placement.}
}
\value{
A point \link[terra:SpatVector-class]{terra::SpatVector} with \code{id}, \code{side}, and \code{distance}
attributes.
}
\description{
Nodes are generated from the finite cells of the target template, then
displaced outward into the computational domain. They are not derived from
arbitrary finite cells in a movement surface.
}
